// Daftar bilangan genap 2 sampai 10
const evenNumbers = [2, 4, 6, 8, 10];

// Menampilkan setiap bilangan genap beserta urutannya
evenNumbers.forEach((number, index) => {
  console.log(`Angka genap ke-${index + 1} : ${number}`);
});
